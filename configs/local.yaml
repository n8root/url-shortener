env: 'local'

http_server:
  host: "localhost"
  port: 8080

postgres:
  host: "localhost"
  port: 5432
  user: "user"
  password: "password"
  db_name: "url_shortener"
  ssl: "disable"

redis:
  host: "localhost"
  port: 6379
  db: 0